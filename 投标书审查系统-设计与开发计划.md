# 投标书内容审查 Web 应用 —— 设计与开发计划

> **版本**: v1.1  
> **编制日期**: 2026-09-12  
> **状态**: 待审批  
> **说明**: 本计划经审批通过后方可启动实施。核心功能采用规则引擎实现（离线可用），LLM 作为可选增强模块，用户按需开启。

---

## 一、需求分析与功能模块划分

### 1.1 项目背景与目标

在政府采购/工程招投标场景中，评标人员需对多家投标公司提交的 Word 投标文件进行逐项符合性审查，核对其是否满足采购文件中的商务条款与技术要求。当前人工审查耗时、易遗漏、标准不统一。

**核心目标**：构建一个无本地数据库的 Web 应用，支持上传 1-5 家公司的投标文件，自动提取内容、与采购文件及项目需求进行智能比对，生成标准化符合性检查表与详细比对报告，并支持以 HTML 格式导出。

### 1.2 用户角色与使用场景

| 角色 | 使用场景 |
|------|---------|
| 审查人员（主用户） | 上传采购文件+项目需求→上传各公司投标文件→配置审查项→查看比对报告→确认结论→导出HTML |
| 复核人员 | 查看审查结果与报告，确认或修正结论 |

### 1.3 功能模块划分

系统划分为 **7 大功能模块**，其中模块⑦为可选增强。边界与交互关系如下：

```
┌─────────────────────────────────────────────────────────────┐
│                     用户操作层（浏览器）                       │
├──────────────┬──────────────┬───────────────┬───────────────┤
│  ① 文件上传   │  ② 审查配置   │   ③ 智能审查   │  ④ 结果展示    │
│  采购文件     │  审查项模板   │  内容提取+检索  │  符合性表格    │
│  项目需求     │  权重/阈值    │  条款比对      │  比对报告      │
│  1-5家投标文件 │  LLM开关+配置 │  偏离识别      │  专业评价      │
└──────────────┴──────────────┴───────────────┴───────────────┘
         │             │              │              │
         └─────────────┴──────────────┴──────────────┘
                        │
              ┌─────────▼──────────┐
              │  ⑤ 报告导出（HTML） │
              │  ⑥ 任务管理（内存）  │
              └────────────────────┘
                        │
              ┌─────────▼──────────┐
              │ ⑦ LLM 增强（可选）   │
              │ 语义条款抽取         │
              │ 语义比对+偏离总结     │
              │ 专业评价生成         │
              └────────────────────┘
```

#### 模块① 文件上传模块
- **输入**：采购文件（.doc/.docx）、项目需求说明（.doc/.docx 或文本）、1-5 家公司投标文件（.doc/.docx）
- **校验**：文件格式、文件大小（≤20MB）、公司数量
- **输出**：解析后的纯文本+结构化内容对象（内存中）
- **交互**：上传完成后进入审查配置区

#### 模块② 审查配置模块
- **功能**：选择审查项模板（报名资格、承诺条款、技术偏离等）、设置匹配阈值、允许自定义审查规则
- **预置审查项**：
  - 报名资格：营业执照、资质证书、业绩证明、财务状况
  - 承诺条款：投标有效期、质量保证、售后服务、违约责任
  - 技术偏离：技术参数响应、技术方案完整性、偏离项标注
- **LLM 增强配置（可选）**：
  - 开关：是否启用 LLM 增强（默认关闭）
  - 服务商选择：OpenAI / 百度千帆 / 智谱 GLM / 本地兼容模型
  - API Key 输入（仅存于内存，不持久化）
  - 增强范围：☑ 条款抽取 ☑ 语义比对 ☑ 偏离总结 ☑ 专业评价
  - 隐私提示：开启后文件文本将发送至所选 LLM 服务商
- **交互**：配置确认后触发智能审查

#### 模块③ 智能审查模块（核心）
- **功能**：
  - 对采购文件与项目需求进行条款抽取，生成"审查基准清单"
  - 对每家投标文件进行章节定位与内容检索
  - 条款级比对（存在性、一致性、偏离度）
  - 生成每家公司的符合性结果
- **输出**：结构化审查结果 JSON

#### 模块④ 结果展示模块
- **符合性检查表格**：每家公司 × 每个审查项的响应状态（符合/不符合/部分符合/未响应）
- **比对报告**：逐条比对详情、原文引用、偏离说明
- **专业评价与建议**：综合评分、风险提示、推荐结论
- **用户确认**：允许用户修改单项结论后再导出

#### 模块⑤ 报告导出模块
- 生成独立 HTML 文件（内联 CSS，离线可打开）
- 包含表格、比对详情、评价结论、导出时间戳

#### 模块⑥ 任务管理模块（无数据库）
- 所有数据存于服务端内存（Map 结构，key=sessionId）
- 会话超时自动清理（默认 30 分钟）
- 支持刷新不丢失当前会话数据

#### 模块⑦ LLM 增强模块（可选）
> **定位**：对规则引擎结果进行语义增强，不改变核心流程。关闭时系统完全离线运行。

- **触发条件**：用户在配置区开启 LLM 开关并填写 API Key
- **增强环节**：
  1. **条款抽取增强**：LLM 阅读采购文件，自动补全规则未覆盖的审查项
  2. **语义比对增强**：对规则判定为"部分符合/偏离"的项，由 LLM 二次判断真实符合性（如识别"90天"≈"三个月"）
  3. **偏离总结增强**：LLM 生成自然语言偏离说明，指出具体差异点
  4. **专业评价增强**：LLM 基于全部审查结果生成专业风险分析与建议结论
- **降级机制**：LLM 调用失败（网络错误/Key 无效/超时）时，自动回退到规则引擎结果，不影响主流程
- **数据安全**：API Key 仅存于当前会话内存，页面关闭即销毁；用户可选择仅发送相关片段而非全文

### 1.4 功能边界（不在本期范围）
- 不进行 PDF 解析（仅支持 .doc/.docx）
- 不存储历史审查记录（无数据库）
- 不做 OCR（仅处理可提取文本）
- 不涉及在线协同编辑

---

## 二、系统架构设计

### 2.1 技术栈选型

| 层级 | 技术选型 | 选型理由 |
|------|---------|---------|
| 前端框架 | **Vue 3 + Vite** | 轻量、响应式、生态成熟，适合快速构建交互界面 |
| UI 组件库 | **Element Plus** | 表格、上传、表单组件完善，契合中后台场景 |
| 前端样式 | 自定义 CSS（赛博朋克霓虹风格） | 贴合用户审美偏好，科技感强 |
| 图表 | **ECharts** | 符合性雷达图、评分柱状图 |
| 后端框架 | **Node.js + Express** | 与前端同语言，文件解析库生态丰富 |
| Word 解析 | **mammoth.js**（.docx）+ **libreoffice 转换**（.doc→.docx） | mammoth 纯 JS 无原生依赖；.doc 通过 LibreOffice headless 转换 |
| 文本检索 | **自研关键词+语义匹配** | 无需重型 NLP 框架，轻量可控 |
| LLM 增强（可选） | **OpenAI / 百度千帆 / 智谱 GLM API** + **统一适配层** | 通过统一接口封装多家服务商，支持切换；用户自带 API Key |
| 报告导出 | **后端渲染 HTML 模板 + 前端下载** | 保证离线可打开 |
| 数据存储 | **进程内内存（Map）** | 满足"无本地数据库"要求，API Key 不持久化 |

### 2.2 系统组件结构

```
┌──────────────────────────────────────────────────────┐
│                    客户端（浏览器）                     │
│  ┌────────────┐  ┌────────────┐  ┌────────────────┐  │
│  │ 文件上传组件 │  │ 审查配置表单│  │ 结果展示+报告  │  │
│  │            │  │ +LLM开关    │  │                │  │
│  └─────┬──────┘  └─────┬──────┘  └───────┬────────┘  │
│        │               │                 │            │
│        └───────────────┼─────────────────┘            │
│                        │ HTTP/JSON                    │
└────────────────────────┼──────────────────────────────┘
                         │
┌────────────────────────▼──────────────────────────────┐
│                  服务端（Node.js）                      │
│  ┌──────────────┐  ┌──────────────┐  ┌─────────────┐ │
│  │ 文件解析层    │  │ 审查引擎层    │  │ 报告生成层  │ │
│  │ mammoth      │  │ 规则条款抽取  │  │ HTML 模板   │ │
│  │ libreoffice  │  │ 检索比对      │  │ 内联CSS     │ │
│  └──────────────┘  └──────┬───────┘  └─────────────┘ │
│                           │ 可选增强调用              │
│  ┌────────────────────────▼─────────────────────────┐ │
│  │         LLM 适配层（统一接口，可选）                │ │
│  │  OpenAI │ 百度千帆 │ 智谱GLM │ 本地兼容模型       │ │
│  └─────────────────────────────────────────────────┘ │
│  ┌─────────────────────────────────────────────────┐ │
│  │           会话内存存储（Map<sessionId, Task>）    │ │
│  │           （含 API Key，不持久化）                 │ │
│  └─────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────┘
                         │
              ┌──────────▼──────────┐
              │  LLM 服务商 API      │
              │  （仅开启增强时调用）  │
              └─────────────────────┘
```

### 2.3 数据流转方案

```
[用户上传文件]
      │
      ▼
[POST /api/upload] ──→ 解析为纯文本 ──→ 存入内存 session
      │
      ▼
[POST /api/review] ──→ 审查引擎（规则版，必走）
      │                ├─ 抽取采购文件条款（基准清单）
      │                ├─ 抽取项目需求条款
      │                ├─ 对每家投标文件进行检索比对
      │                └─ 生成符合性结果 + 评价
      │
      ├─ [LLM 关闭] → 直接使用规则结果
      │
      └─ [LLM 开启] → LLM 适配层（可选增强）
                       ├─ 条款抽取补全
                       ├─ 语义比对二次判定
                       ├─ 偏离说明生成
                       └─ 专业评价重写
                       │（失败自动回退规则结果）
                       ▼
                  合并增强结果
      ▼
[GET /api/result/:sessionId] ──→ 前端渲染表格/报告
      │
      ▼
[POST /api/export] ──→ 生成独立 HTML ──→ 浏览器下载
```

**关键数据结构（内存中）**：

```json
{
  "sessionId": "uuid",
  "procurementDoc": { "name": "采购文件.docx", "text": "...", "clauses": [...] },
  "projectRequirements": { "text": "...", "clauses": [...] },
  "bidders": [
    {
      "id": "b1",
      "name": "公司A",
      "fileName": "公司A投标文件.docx",
      "text": "...",
      "sections": [...]
    }
  ],
  "reviewConfig": {
    "items": [...],
    "threshold": 0.75,
    "llm": {
      "enabled": false,
      "provider": "openai",
      "apiKey": "sk-***(仅内存)",
      "scope": ["clauseExtract", "semanticMatch", "deviation", "evaluation"]
    }
  },
  "reviewResult": {
    "complianceTable": [...],
    "comparisonDetails": [...],
    "evaluation": { "score": 0, "risks": [...], "conclusion": "..." },
    "llmEnhanced": false
  },
  "createdAt": 1789224627000
}
```

---

## 三、UI/UX 设计方案

### 3.1 整体风格

- **主题**：赛博朋克科技风，深色背景 + 霓虹高亮（青色 #00f0ff / 品红 #ff00aa / 荧光绿 #00ff88）
- **布局**：单页应用（SPA），顶部导航 + 步骤式主内容区
- **字体**：等宽字体增强科技感（JetBrains Mono / Consolas）

### 3.2 四大功能区布局

#### 区① 文件上传区
```
┌─────────────────────────────────────────────────────┐
│  ▌ 文件上传                    [步骤 1/4]            │
├─────────────────────────────────────────────────────┤
│  ┌──────────────┐  ┌──────────────┐  ┌────────────┐│
│  │  采购文件      │  │  项目需求      │  │ 投标文件   ││
│  │  （.doc/.docx）│  │ （可选文本）   │  │ 1-5家     ││
│  │  [拖拽/点击]   │  │  [拖拽/点击]   │  │ [+添加]   ││
│  └──────────────┘  └──────────────┘  └────────────┘│
│                                                     │
│  已上传：                                          │
│  ☑ 采购文件2026.docx        [✕]                     │
│  ☑ 公司A_投标书.docx         [✕]  添加第2家...       │
└─────────────────────────────────────────────────────┘
```

#### 区② 审查配置区
```
┌─────────────────────────────────────────────────────┐
│  ▌ 审查配置                    [步骤 2/4]            │
├─────────────────────────────────────────────────────┤
│  ☑ 报名资格审查                                    │
│     ☑ 营业执照   ☑ 资质证书   ☑ 业绩证明  ☑ 财务   │
│  ☑ 承诺条款审查                                    │
│     ☑ 投标有效期 ☑ 质量保证   ☑ 售后服务  ☑ 违约   │
│  ☑ 技术偏离审查                                    │
│     ☑ 参数响应   ☑ 方案完整   ☑ 偏离标注            │
│                                                     │
│  匹配阈值：[████████░░] 75%                         │
│                                                     │
│  ┌─ 🤖 AI 增强（可选，默认关闭）───────────────────┐ │
│  │  [ ] 启用 LLM 增强                              │ │
│  │  服务商：[OpenAI      ▼]  API Key：[________]   │ │
│  │  增强范围：☑条款抽取 ☑语义比对 ☑偏离 ☑评价      │ │
│  │  ⚠ 开启后文件文本将发送至所选服务商              │ │
│  └─────────────────────────────────────────────────┘ │
│                                                     │
│  [开始审查]                                         │
└─────────────────────────────────────────────────────┘
```

#### 区③ 结果展示区
```
┌─────────────────────────────────────────────────────┐
│  ▌ 审查结果                    [步骤 3/4]            │
├─────────────────────────────────────────────────────┤
│  ┌─符合性检查表格─────────────────────────────────┐ │
│  │ 审查项      │ 公司A │ 公司B │ 公司C │ 公司D     │ │
│  │ 营业执照    │  ✓符合 │  ✓符合 │ ✗缺失 │ ✓符合   │ │
│  │ 投标有效期  │  ✓符合 │ △偏离  │ ✓符合 │ ✓符合   │ │
│  │ 技术参数响应│  ✓符合 │  ✓符合 │ △偏离 │ ✗未响应 │ │
│  └─────────────────────────────────────────────────┘ │
│                                                     │
│  [选中行查看比对详情]  → 右侧/下方展示原文引用       │
│                                                     │
│  ┌─专业评价───────────────────────────────────────┐ │
│  │ 综合评分：███░░ 72分                            │ │
│  │ 风险提示：公司C缺少营业执照，公司D未响应技术参数 │ │
│  │ 建议结论：[推荐/有条件推荐/不推荐] （可编辑）     │ │
│  └─────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────┘
```

#### 区④ 报告导出区
```
┌─────────────────────────────────────────────────────┐
│  ▌ 报告导出                    [步骤 4/4]            │
├─────────────────────────────────────────────────────┤
│  导出内容：☑符合性表格 ☑比对详情 ☑专业评价          │
│  [生成 HTML 报告并下载]                              │
│                                                     │
│  预览：报告将包含完整审查数据，离线可打开             │
└─────────────────────────────────────────────────────┘
```

### 3.3 交互流程

1. **步骤引导**：顶部进度条指示 4 步进度，已完成步骤可回退
2. **实时反馈**：文件上传进度、审查加载动画（霓虹加载条）
3. **状态色标**：✓符合（绿）/ △部分符合（黄）/ ✗不符合（红）/ ○未响应（灰）
4. **结果可编辑**：用户可点击单元格修改状态，修改后评价自动重算

---

## 四、核心技术实现路径

### 4.1 Word 文件解析与内容提取

**目标**：将 .doc/.docx 转为可检索的结构化文本。

#### .docx 解析（mammoth.js）
```javascript
const mammoth = require('mammoth');
const result = await mammoth.extractRawText({ path: filePath });
// result.value -> 纯文本
// 同时用 extractRawText + 提取标题层级（通过文档样式）
```

**增强**：使用 `mammoth.convertToHtml` 保留段落结构，再从 HTML 中提取标题（h1-h6）作为章节锚点，便于后续检索定位。

#### .doc 解析（旧格式）
**方案**：通过 LibreOffice headless 模式将 .doc 转为 .docx，再用 mammoth 解析。

```javascript
const { execFile } = require('child_process');
execFile('soffice', [
  '--headless', '--convert-to', 'docx',
  '--outdir', tmpDir, filePath
], (err) => { /* 转换后用 mammoth 解析 */ });
```

**降级方案**：若环境无 LibreOffice，提示用户手动另存为 .docx。

#### 章节结构化
解析后输出结构：
```json
{
  "rawText": "全文...",
  "sections": [
    { "level": 1, "title": "一、投标函", "content": "..." },
    { "level": 2, "title": "1.1 投标报价", "content": "..." }
  ]
}
```

### 4.2 文本检索与比对算法

#### 第一步：基准条款抽取
从采购文件与项目需求中抽取审查基准项：
- **规则抽取**：基于关键词正则（如"投标有效期为\d+天"、"资质要求"、"业绩要求"）
- **章节定位**：识别"资格要求"、"商务条款"、"技术要求"等章节标题，提取其下内容
- 输出：`{ clauseId, category, keyword, expectedText, section }`

#### 第二步：投标文件检索
对每个基准条款，在每家投标文件中检索响应内容：

```javascript
// 多层匹配策略
function matchClause(clause, bidderText, sections) {
  // 1. 关键词命中（精确/包含）
  const exactMatch = bidderText.includes(clause.keyword);
  
  // 2. 同义词扩展（如"营业执照"≈"工商执照"）
  const synonymHit = clause.synonyms.some(s => bidderText.includes(s));
  
  // 3. 章节定位（在相关章节内搜索提高精度）
  const section = sections.find(s => 
    s.title.includes(clause.categorySection)
  );
  
  // 4. 正则模式匹配（如"有效期.*\d+.*天"）
  const patternHit = clause.pattern 
    ? new RegExp(clause.pattern).test(bidderText) 
    : false;
  
  return { exactMatch, synonymHit, patternHit, matchedText: ... };
}
```

#### 第三步：符合性判定
```
综合得分 = 0.4×关键词命中 + 0.3×章节定位 + 0.3×模式匹配

得分 ≥ 阈值（默认0.75）→ 符合
0.4 ≤ 得分 < 0.75      → 部分符合/偏离
得分 < 0.4 且无任何命中 → 未响应
关键字词明确矛盾       → 不符合
```

#### 第四步：偏离识别
对"部分符合"项，抽取投标文件原文片段，与基准条款做差异比对，生成偏离说明。

### 4.3 符合性检查表格生成逻辑

```javascript
function generateComplianceTable(clauses, bidders, reviewConfig) {
  const table = {
    headers: ['审查项', '类别', ...bidders.map(b => b.name)],
    rows: clauses.map(clause => ({
      item: clause.title,
      category: clause.category,
      cells: bidders.map(bidder => {
        const result = matchClause(clause, bidder);
        return {
          status: result.status,       // 符合/不符合/部分符合/未响应
          score: result.score,
          evidence: result.matchedText,
          deviation: result.deviation
        };
      })
    }))
  };
  return table;
}
```

表格渲染时：
- 单元格按状态着色
- 点击单元格弹出比对详情（基准文本 vs 投标原文）

### 4.4 报告生成与 HTML 导出

**方案**：后端使用模板引擎（如 `ejs`）渲染完整 HTML，内联所有 CSS 与数据，生成独立文件。

```javascript
// 后端：渲染 HTML 模板
const html = await ejs.renderFile('report-template.ejs', {
  title: '投标书符合性审查报告',
  generatedAt: new Date().toLocaleString(),
  procurementName: task.procurementDoc.name,
  bidders: task.bidders,
  complianceTable: task.reviewResult.complianceTable,
  comparisonDetails: task.reviewResult.comparisonDetails,
  evaluation: task.reviewResult.evaluation
});

// 设置响应头触发下载
res.setHeader('Content-Type', 'text/html; charset=utf-8');
res.setHeader('Content-Disposition', 'attachment; filename="审查报告.html"');
res.send(html);
```

**模板要点**：
- 内联 CSS（不引用外部资源，保证离线可打开）
- 表格样式与前端展示一致
- 包含页眉（项目名、导出时间）、符合性表格、逐条比对详情、专业评价与建议结论页脚

**前端下载**：
```javascript
const blob = new Blob([htmlString], { type: 'text/html' });
const url = URL.createObjectURL(blob);
const a = document.createElement('a');
a.href = url; a.download = '审查报告.html'; a.click();
```

### 4.5 LLM 增强模块实现（可选）

#### 设计原则
- **可插拔**：LLM 模块独立封装，不侵入规则引擎主流程
- **默认关闭**：系统开箱即用规则版，用户主动开启才调用 LLM
- **失败降级**：任何 LLM 调用异常都不阻断审查，回退规则结果
- **隐私可控**：API Key 仅存内存，不写日志、不持久化

#### 统一适配层
通过统一接口封装多家 LLM 服务商，便于切换：

```javascript
// llm/adapter.js
class LLMAdapter {
  constructor(provider, apiKey) {
    this.provider = provider;
    this.apiKey = apiKey;
  }

  async chat(messages, options = {}) {
    switch (this.provider) {
      case 'openai':   return this._callOpenAI(messages, options);
      case 'qianfan':  return this._callQianfan(messages, options);
      case 'glm':      return this._callGLM(messages, options);
      case 'local':    return this._callLocal(messages, options);
      default: throw new Error('Unsupported provider');
    }
  }
  // 各服务商具体实现...
}
```

#### 四大增强场景的 Prompt 设计

**1. 条款抽取增强**
```
你是招投标审查专家。请从以下采购文件中抽取所有资格审查、商务条款、
技术要求项，输出 JSON 数组，每项包含：名称、类别、关键要求文本。
采购文件内容：{procurementText}
```

**2. 语义比对增强**（仅对规则判定为"部分符合/偏离"的项调用）
```
请判断投标文件内容是否符合采购文件要求。
采购要求：{expectedText}
投标响应：{bidderText}
输出 JSON：{ "status": "符合|不符合|部分符合", "reason": "判断理由" }
```

**3. 偏离总结增强**
```
请用一句话总结投标文件与采购要求的具体偏离点。
采购要求：{expectedText}
投标响应：{bidderText}
```

**4. 专业评价增强**
```
你是资深评标专家。基于以下符合性审查结果，生成专业评价：
1. 各公司符合性概况
2. 主要风险点
3. 推荐结论（推荐/有条件推荐/不推荐）及理由
审查结果：{complianceTable}
```

#### 调用流程与降级
```javascript
async function enhanceWithLLM(task, scope) {
  if (!task.reviewConfig.llm.enabled) return task.reviewResult;

  const llm = new LLMAdapter(provider, apiKey);
  
  try {
    // 1. 条款抽取补全
    if (scope.includes('clauseExtract')) {
      const extraClauses = await llm.chat([...]);
      task.clauses = mergeClauses(task.clauses, extraClauses);
    }
    
    // 2. 语义比对（仅对低置信度项）
    if (scope.includes('semanticMatch')) {
      for (const cell of lowConfidenceCells) {
        const llmResult = await llm.chat([...]);
        cell.status = llmResult.status; // 用 LLM 结果覆盖
      }
    }
    
    // 3. 偏离总结 + 4. 专业评价
    // ...
    
    task.reviewResult.llmEnhanced = true;
  } catch (err) {
    // 降级：保留规则结果，标记增强失败
    task.reviewResult.llmEnhanced = false;
    task.reviewResult.llmError = err.message;
  }
  
  return task.reviewResult;
}
```

#### 成本与性能控制
- **片段发送**：仅发送相关章节片段，不发送全文，降低 Token 消耗
- **批量调用**：多个比对项合并为一次 LLM 调用，减少请求次数
- **超时控制**：单次 LLM 调用超时 15 秒，整体增强超时 60 秒
- **缓存**：相同条款+相同投标文本的比对结果缓存，避免重复调用

---

## 五、开发阶段划分与时间规划

> 总工期预估：**7 周**（按单人开发估算，可并行压缩）。其中 P5（LLM 增强）为可选阶段，若不启用 LLM 可跳过，总工期回到 6 周。

| 阶段 | 周次 | 任务 | 产出物 |
|------|------|------|--------|
| **P0 环境搭建** | W1 | 项目脚手架（Vue3+Vite + Express）、依赖安装、目录结构、Hello World 联调 | 可运行的前后端骨架 |
| **P1 文件解析** | W1-W2 | mammoth 集成、.doc→.docx 转换、章节结构化抽取、上传接口 | 文件解析模块 + 上传接口 |
| **P2 审查引擎** | W2-W3 | 基准条款抽取规则库、检索比对算法、符合性判定、偏离识别 | 审查引擎 + 单元测试 |
| **P3 前端界面** | W3-W4 | 上传区、配置区、结果表格、评价区 UI，赛博朋克主题样式 | 完整前端页面 |
| **P4 报告导出** | W4 | HTML 模板、内联 CSS、导出接口、下载交互 | 报告导出功能 |
| **P5 LLM 增强（可选）** | W5 | LLM 统一适配层、四大增强场景 Prompt、调用流程与降级、配置区 UI | LLM 增强模块 |
| **P6 集成联调** | W6 | 前后端联调、真实标书测试、阈值调优、边界处理、LLM 降级测试 | 可运行的完整系统 |
| **P7 测试验收** | W7 | 单元测试补全、集成测试、用户验收测试、Bug 修复 | 测试报告 + 交付版本 |

### 关键里程碑
- **M1（W2 末）**：文件解析可用，能正确提取 5 份样例标书内容
- **M2（W3 末）**：审查引擎输出符合性表格，准确率 ≥ 80%
- **M3（W4 末）**：全流程贯通（规则版），可上传→审查→展示→导出
- **M4（W5 末）**：LLM 增强模块可用，开启后准确率提升且失败可降级
- **M5（W7 末）**：验收通过，交付

---

## 六、测试策略

### 6.1 单元测试
- **框架**：Vitest（前端）+ Jest（后端）
- **覆盖范围**：
  - 文件解析：mammoth 输出文本正确性、章节切分准确性
  - 检索算法：关键词命中、同义词匹配、模式匹配各分支
  - 符合性判定：各分数区间对应正确状态
  - 表格生成：行列结构、状态映射
- **覆盖率目标**：核心逻辑 ≥ 80%

### 6.2 集成测试
- **场景**：
  1. 上传 1 家完整标书 → 全项符合
  2. 上传 3 家标书（含缺失项、偏离项）→ 正确识别不符合
  3. 上传 .doc 旧格式 → 自动转换并解析
  4. 超大文件 → 正确报错提示
  5. 会话超时 → 数据清理
  6. **LLM 关闭** → 规则版正常输出，无网络请求
  7. **LLM 开启（正常）** → 增强结果覆盖规则结果，`llmEnhanced=true`
  8. **LLM 开启（Key 无效）** → 自动降级到规则结果，`llmEnhanced=false` 且提示错误
  9. **LLM 开启（网络超时）** → 60 秒超时后降级，不阻断主流程
- **验证点**：接口返回数据结构、状态码、错误处理、降级逻辑

### 6.3 用户验收测试（UAT）
- **测试数据**：准备 3 套真实（脱敏）采购文件+投标文件
- **验收标准**：
  - 符合性识别准确率 ≥ 85%
  - 单次审查（5 家）耗时 ≤ 30 秒
  - 导出 HTML 在 Chrome/Edge 离线打开无样式丢失
  - 界面无明显错位，操作流畅

---

## 七、项目风险评估与应对措施

| 风险项 | 等级 | 影响 | 应对措施 |
|--------|------|------|---------|
| .doc 解析依赖 LibreOffice，部署环境可能缺失 | 高 | 旧格式标书无法解析 | 1. 启动时检测 LibreOffice 并提示安装；2. 前端引导用户另存为 .docx；3. 提供在线转换备选 |
| 采购文件条款格式多样，规则抽取准确率不足 | 高 | 审查基准清单遗漏 | 1. 预置通用规则库（资格/商务/技术）；2. 允许用户手动增删审查项；3. 持续积累规则模板 |
| 投标文件章节命名不统一，检索定位困难 | 中 | 匹配精度下降 | 1. 同义词词典扩展；2. 全文兜底搜索；3. 降低章节定位权重 |
| 内存存储在进程重启后丢失 | 中 | 用户数据丢失 | 1. 明确告知用户数据不持久化；2. 提供"导出 JSON 快照"供下次导入；3. 后续版本可接对象存储 |
| 大文件解析阻塞事件循环 | 中 | 界面卡顿 | 1. 文件大小限制（20MB）；2. 解析任务放入 worker 线程；3. 超时机制 |
| 专业评价规则主观性强 | 中 | 结论不被用户认可 | 1. 评价逻辑透明化（展示评分明细）；2. 结论可编辑确认；3. 提供多套评价模板 |
| 无数据库导致多用户会话冲突 | 低 | 并发数据错乱 | 1. sessionId 隔离；2. 内存 Map 按 sessionId 分片；3. 单实例部署 |
| LLM 调用失败影响审查流程 | 高 | 审查中断或结果异常 | 1. 规则引擎为主路径，LLM 仅增强；2. try-catch 自动降级；3. 超时控制+结果缓存 |
| LLM 返回结果不可控（幻觉） | 中 | 符合性误判 | 1. LLM 仅对低置信度项二次判定；2. 要求输出结构化 JSON 并校验；3. 保留规则结果供用户核对 |
| LLM API 费用不可控 | 中 | 使用成本超预期 | 1. 仅发送相关片段不发全文；2. 批量调用减少请求；3. 结果缓存；4. 前端提示预计 Token 消耗 |
| 标书文本上传至 LLM 服务商的隐私风险 | 高 | 商业机密泄露 | 1. 默认关闭 LLM，用户主动开启；2. 显著隐私提示；3. 支持仅发送片段；4. 提供本地兼容模型选项 |
| API Key 泄露风险 | 中 | 账号被盗用 | 1. Key 仅存内存不持久化；2. 不写入日志；3. 页面关闭即销毁；4. 支持环境变量预置 |

---

## 审批与启动

本计划经审批通过后，将按 **P0→P6** 阶段顺序启动实施。实施过程中如遇需求变更或重大技术阻碍，将及时更新本计划并重新确认。

---
*文档结束*
